[gcode_macro FILAMENT_CHANGE]
description: "Pause the print, move to a safe position, unload filament, call next macro, and resume"

gcode:
    # Pause the print
    PAUSE

    # Execute the filament change state macro
    FILAMENT_CHANGE_STATE2
